<phpunit bootstrap="./TestHelper.php">
    <testsuite name="ZLayer Extension - Testsuite">
        <directory>./</directory>
    </testsuite>

    <!-- Enable this for proper unit testing code coverage reports
    <filter>
        <whitelist>
            <directory suffix=".php">../library/ZLayer</directory>
        </whitelist>
    </filter>
    -->
</phpunit>
